# PDF 转 Word OCR 版

适用于 Windows 10/11 64 位。EXE 内置 Python、RapidOCR 中文/英文模型和 CPU 推理引擎，无需单独安装 Python 或下载识别模型。文档在电脑本地转换。

## 下载与使用
在 Actions 中打开成功的 Build Windows EXE，下载 PDF-to-Word-OCR-Windows-x64，解压后运行 PDF-to-Word-OCR.exe。
选择 PDF，默认勾选“扫描件 OCR”，点击“转换并保存 Word”。
OCR 按页识别全部可见文字，逐行输出可编辑段落。可选择附上原页图片，用于核对和保留原图信息；图片本身不变成可编辑对象。
普通文字型 PDF 若需保留原排版，请取消扫描件 OCR，使用原有转换功能。

## 识别范围
支持清晰的中文、英文印刷文字和数字。扫描件、文字和图片混合的 PDF 均可使用 OCR 模式。
不保证手写、模糊、倾斜、旋转或复杂多栏文档的识别准确率和阅读顺序。扫描表格、公式暂不重建为可编辑表格或公式。
OCR 出错的文字需要人工校对。若某页未识别到文字，软件会提示页码，并自动保留原页图片供核对。
加密 PDF 请先解密。转换期间界面显示正在处理的页码。
保存成功前不会替换目标文件；建议另存到新的 DOCX 文件。
大文件附带原图时会占用较多内存，必要时分批转换。

## 验证
云端流程验证普通 PDF 文字和图片转换，并生成没有文字层的中英文扫描 PDF，检查 OCR 输出含有指定中文和数字、原图仍保留。
源码和打包后的 EXE 都执行这些测试。测试环境是 Windows Server 2022，尚未在用户的 Windows 10 电脑实测。
EXE 未数字签名。

## 源码
安装 requirements.txt 后运行 python app.py。
使用 pdf2docx、PyMuPDF、python-docx、RapidOCR、ONNX Runtime 和 PyInstaller。各第三方组件遵循各自许可证。
