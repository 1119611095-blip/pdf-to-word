# PDF 转 Word

面向 Windows 10/11 64 位的中文桌面应用。下载打包后的 EXE，无需安装 Python 或 Microsoft Word 即可转换。

## 下载
打开仓库的 Actions 页面，选择成功的 Build Windows EXE 运行，在 Artifacts 中下载 PDF-to-Word-Windows-x64（需要登录 GitHub）。
解压 ZIP，双击 PDF-to-Word.exe。

## 使用
1. 点击“选择 PDF”。
2. 点击“转换并保存 Word”，选择 DOCX 保存位置。
3. 等待完成，使用 Word 或 WPS 打开生成的文件。

文件在本机转换，应用不会上传 PDF。普通 PDF 的文字可编辑，图片和表格尽量保留。复杂版面可能需要手动调整，不保证一比一还原。
此版本没有 OCR：扫描图片中的文字不会变成可编辑文字。加密 PDF 需要先解密。
保存失败时不会主动删除已有的目标文档；成功转换后才替换目标文件。
转换期间请等待完成后关闭程序。

## 构建和验证
GitHub Actions 使用 Windows Server 2022 和 Python 3.11 x64 打包。
流程先验证源码转换，再运行打包后的 EXE，检查 DOCX 中存在可编辑文字和图片。
这些检查不代表已经在真实 Windows 10 桌面完成全部界面和复杂文档测试。
EXE 没有数字签名。

## 源码运行
安装 requirements.txt 中的依赖后运行 python app.py。
转换使用 pdf2docx、PyMuPDF 和 python-docx，打包使用 PyInstaller；相关第三方组件按各自许可证提供。
